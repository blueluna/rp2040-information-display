[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --probe 2e8a:000c --chip RP2040"
[build]
target = "thumbv6m-none-eabi"

[env]
DEFMT_LOG = "info"
WIFI_SSID = ""
WIFI_PASSWORD = ""
